name: movies_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # نستخدم 5.0.0 لحل مشكلة CarouselController
  carousel_slider: ^5.0.0
  http: ^1.1.0
  shimmer: ^3.0.0
  animate_do: ^3.3.4
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.8
  flutter_lints: ^2.0.0

dependency_overrides:
  # سنستخدم إصدار ثابت ومستقر جداً
  win32: 5.5.4


flutter:
  uses-material-design: true
  assets:
    - assets/image/
